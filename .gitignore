# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# CSX large artifacts — keep in HF Hub, not git
TEXTUREMAP/data/raw/
TEXTUREMAP/data/processed/
TEXTUREMAP/data/datasets/
TEXTUREMAP/checkpoints/
TEXTUREMAP/outputs/

# StyleGAN generation outputs (snapshots, generated media, sample sheets)
# scoped to our pipeline dirs so the stylegan3 submodule's own assets are untouched
TEXTUREMAP/results/
TEXTUREMAP/samples/
TEXTUREMAP/scripts/**/*.pkl
TEXTUREMAP/src/**/*.pkl
TEXTUREMAP/**/network-snapshot-*.pkl
TEXTUREMAP/**/fakes*.png
TEXTUREMAP/**/*_contact.jpg

# macOS
.DS_Store
